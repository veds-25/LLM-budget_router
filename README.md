# LLM-budget_router
In this project, we will build a RL based routing system that learns how to allocate a fixed inference budget across a stream of incoming requests. Given the characteristics of each query and the remaining budget, the agent must decide which model to use in order to maximise overall response quality throughout the entire episode.
